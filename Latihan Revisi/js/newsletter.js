document.addEventListener("DOMContentLoaded", () => {
    const chackbox = document.getElementById("langganan");

    checkbox.addEventListener("change", () => {
        if (checkbox.checked) {
            alert("Terima kasih telah berlangganan!");
        } else {
            alert("Langganan dibatalkan.");
        }
    });
});